diff --git a/build.sbt b/build.sbt
index c427cea..366637b 100644
--- a/build.sbt
+++ b/build.sbt
@@ -45,10 +45,10 @@ crossScalaVersions := Seq("2.12.6", "2.11.12")
 // Provide a managed dependency on X if -DXVersion="" is supplied on the command line.
 // The following are the default development versions, not the "release" versions.
 val defaultVersions = Map(
-  "chisel3" -> "3.2-SNAPSHOT",
-  "firrtl" -> "1.2-SNAPSHOT",
-  "firrtl-interpreter" -> "1.2-SNAPSHOT",
-  "treadle" -> "1.1-SNAPSHOT"
+  "chisel3" -> "3.2+",
+  "firrtl" -> "1.2+",
+  "firrtl-interpreter" -> "1.2+",
+  "treadle" -> "1.1+"
   )
 
 libraryDependencies ++= Seq("chisel3","firrtl","firrtl-interpreter", "treadle").map { dep: String =>
